databases:
  - name: nexivoreach-db
    plan: free
    databaseName: nexivoreach
    user: nexivoreach

services:
  - type: web
    name: nexivoreach
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: GROQ_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: SERPER_API_KEY
        sync: false
      - key: TAVILY_API_KEY
        sync: false
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      # Live multi-device data: managed Postgres (not ephemeral SQLite).
      - key: DATABASE_URL
        fromDatabase:
          name: nexivoreach-db
          property: connectionString
      - key: PYTHON_ENV
        value: production
      - key: APP_URL
        sync: false
      - key: GOOGLE_REDIRECT_URI
        sync: false
      - key: GOOGLE_SHEETS_SERVICE_ACCOUNT_JSON
        sync: false
      - key: GOOGLE_SHEETS_SPREADSHEET_ID
        sync: false
